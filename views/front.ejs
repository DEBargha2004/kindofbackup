<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <% if(data[0]['mimetype'] == 'video/mp4') { %>
        <video height="400" controls>
            <source src="/<%=data[0]['im_name']%>" type="<%=data[0]['mimetype']%>">
        </video>
    <%}else if(data[0]['mimetype'] == 'application/pdf'){%>
        <embed src="/<%=data[0]['im_name']%>" type="" height="640px" width="1000px">
    <% }else{%>
        <a href="#" download="/<%=data[0]['im_name']%>"><img src="/<%=data[0]['im_name']%>"  alt="profile_image"></a>
    <%}%>
</body>
</html>